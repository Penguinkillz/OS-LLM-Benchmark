import { BookOpen, ExternalLink, Info } from "lucide-react";
import Link from "next/link";

const hint = "text-muted-foreground";
const kbd = "font-mono text-[0.7rem] text-accent bg-muted/40 rounded px-1 py-0.5 border border-border/50";

export function AddModelTutorial() {
  return (
    <aside className="w-full rounded-2xl border border-border/60 bg-card/40 p-6 sm:p-8 text-sm text-foreground">
      <div className="flex flex-col gap-4 sm:flex-row sm:items-start sm:gap-6">
        <div className="flex h-10 w-10 shrink-0 items-center justify-center rounded-lg border border-primary/25 bg-primary/10 text-primary sm:mt-0.5">
          <Info className="h-5 w-5" aria-hidden />
        </div>
        <div className="min-w-0 flex-1 space-y-1">
          <h3 className="font-display text-lg font-bold text-foreground">How to add a model</h3>
          <p className={hint}>
            The web app only stores <span className="text-foreground">metadata</span>. The{" "}
            <span className="text-foreground">Python worker</span> loads weights from Hugging Face or a local folder when
            you start a run.
          </p>
        </div>
      </div>

      <div className="mt-8 grid gap-8 lg:grid-cols-3 lg:gap-8">
        <section>
          <h4 className="text-xs font-semibold uppercase tracking-wider text-primary">Required</h4>
          <ul className="mt-3 list-disc space-y-2 pl-5 text-muted-foreground">
            <li>
              <span className="text-foreground">Display name</span> is required (e.g. a short label in the UI).
            </li>
            <li>
              <span className="text-foreground">At least one load target</span> is required so a run can load weights:
              <ul className="mt-1.5 list-[circle] space-y-1.5 pl-4">
                <li>
                  <span className="text-foreground">Hugging Face model id</span> in <span className={kbd}>org/model</span> form, or
                </li>
                <li>
                  <span className="text-foreground">Source</span> set to the same <span className={kbd}>org/model</span> (if you
                  do not use the dedicated field), or
                </li>
                <li>
                  <span className="text-foreground">Local model folder</span> on the machine running the worker.
                </li>
              </ul>
            </li>
          </ul>
        </section>

        <section>
          <h4 className="text-xs font-semibold uppercase tracking-wider text-primary">Where to find the Hugging Face id</h4>
          <ol className="mt-3 list-decimal space-y-1.5 pl-5 text-muted-foreground">
            <li>
              Open{" "}
              <a
                href="https://huggingface.co/models?pipeline_tag=text-generation&sort=trending"
                target="_blank"
                rel="noreferrer"
                className="inline-flex items-center gap-1 text-primary hover:underline"
              >
                Hugging Face
                <ExternalLink className="h-3 w-3" />
              </a>{" "}
              and pick a text-generation (causal LM) model.
            </li>
            <li>
              On the model page, the id is <span className="text-foreground">owner/model</span> in the URL, e.g.{" "}
              <span className={kbd}>Qwen/Qwen2.5-0.5B-Instruct</span>.
            </li>
            <li>Paste it into <span className="text-foreground">Hugging Face model id</span> or <span className="text-foreground">Source</span> (as <span className={kbd}>org/model</span> only).</li>
          </ol>
        </section>

        <section>
          <h4 className="text-xs font-semibold uppercase tracking-wider text-primary">Optional fields</h4>
          <ul className="mt-3 list-disc space-y-2 pl-5 text-muted-foreground">
            <li>
              <span className="text-foreground">Hugging Face model id</span> when you want a dedicated, explicit field for
              the Hub. If you also set local path, the worker prefers local.
            </li>
            <li>
              <span className="text-foreground">Local model folder</span> for on-disk checkpoints: must look like a standard HF
              folder. The path is on the worker host (or in Docker with a volume).
            </li>
            <li>
              <span className="text-foreground">Source</span> for a label, or a Hub id in <span className={kbd}>org/model</span> if you did
              not use the dedicated field.
            </li>
            <li>
              <span className="text-foreground">Slug</span> for a stable API/URL key; autogenerated if empty.
            </li>
          </ul>
        </section>
      </div>

      <p className="mt-6 rounded-lg border border-border/40 bg-background/30 px-4 py-3 text-xs text-muted-foreground/90">
        <span className="text-foreground/90">Private or gated models:</span> set{" "}
        <span className={kbd}>HUGGING_FACE_HUB_TOKEN</span> (or <span className={kbd}>HF_TOKEN</span>) in the worker
        environment.
      </p>

      <div className="mt-4 flex flex-wrap gap-2">
        <Link
          href="/docs/methodology"
          className="inline-flex items-center gap-1.5 rounded-lg border border-border bg-background/50 px-3 py-1.5 text-xs hover:border-primary/30"
        >
          <BookOpen className="h-3.5 w-3.5" />
          Methodology
        </Link>
        <Link
          href="/dashboard/parameters"
          className="inline-flex items-center gap-1.5 rounded-lg border border-border bg-background/50 px-3 py-1.5 text-xs hover:border-primary/30"
        >
          Run parameters
        </Link>
      </div>
    </aside>
  );
}
